import { createGlobalTheme } from '@vanilla-extract/css'

export const windowHeightVars = createGlobalTheme(':root', {
  height: ''
})

export const windowWidthVars = createGlobalTheme(':root', {
  width: ''
})
