mutation ForgotPassword($email: EmailAddress!) {
  forgotPassword(input: { email: $email }) {
    user {
      personal {
        email
        id
        name
      }
      settings {
        currency
      }
    }
  }
}
